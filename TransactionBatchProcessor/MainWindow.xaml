<Window x:Class="TransactionBatchProcessor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Transaction Batch Processor" Height="420" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center" Margin="0,0,0,8">
            <Button Name="BtnSelect" Width="100" Click="BtnSelect_Click">Select CSV</Button>
            <TextBox Name="TxtFilePath" Margin="8,0,0,0" Width="600" IsReadOnly="True"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Center" Margin="0,0,0,8">
            <Button Name="BtnProcess" Width="100" Click="BtnProcess_Click">Process</Button>
            <ProgressBar Name="Progress" Width="600" Height="20" Margin="8,0,0,0" Minimum="0" Maximum="100" />
        </StackPanel>

        <TextBox Name="TxtLog" Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" AcceptsReturn="True" TextWrapping="Wrap" IsReadOnly="True"/>
    </Grid>
</Window>
